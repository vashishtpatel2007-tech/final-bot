import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
    title: "BMSIT Academic Bot — AI Study Assistant",
    description:
        "AI-powered academic assistant for BMSIT engineering students. Get answers from your syllabus, timetables, notes, and question papers.",
};

export default function RootLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <html lang="en">
            <body className={inter.className}>{children}</body>
        </html>
    );
}
